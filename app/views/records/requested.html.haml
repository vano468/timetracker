%h1 Record
%h2 Requested

- if @records.present?
  %table.table.table-responsive
    %thead
      %th ID
      %th Type
      %th Status
      %th
      %th
    - @records.each do |r|
      %tr
        %td= link_to r.id, record_path(r)
        %td= r.type
        %td= (r.boss_approved == nil ? 'pending' : r.boss_approved)
        %td= link_to 'Add comment', new_record_comment_path(r)
        %td= link_to 'Delete', record_path(r), method: :delete