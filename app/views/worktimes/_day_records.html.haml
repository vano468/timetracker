.day-records
  .day-header
    %span.day-title
      = day_title_format day
    = button_tag 'Add record', class: 'day-add', id: 'day-record-btn'
  .day-body
    - @worktimes.each do |row|
      .day-row
        = "#{only_time row.date_from} - #{only_time row.date_to}"

= render 'modal'